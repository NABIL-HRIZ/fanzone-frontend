name: FanZone Frontend CI

on:
  push:
    branches:
      - "main"

jobs:
  build:
    # 🏃‍ بيئة التشغيل
    runs-on: ubuntu-latest

    # ⚙️ إعدادات العمل: قمنا بتعديل المسار هنا
    defaults:
      run:
        # ⬅️ المسار المصحح: حذف النقطة والنقطة المائلة (./)
        working-directory: front_end

    steps:
      # -------------------
      # 📥 1. Checkout code
      # -------------------
      - name: Checkout repository
        # هذا الإجراء يجلب كل محتويات المستودع إلى بيئة التشغيل
        uses: actions/checkout@v3

      # -------------------
      # 🟧 2. Setup Node.js
      # -------------------
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20.x"

      # -------------------
      # 📦 3. Install dependencies
      # -------------------
      - name: Install React dependencies
        # سيتم تشغيله الآن داخل مجلد 'front_end'
        run: npm install

      # -------------------
      # 🛠️ 4. Build React/Vite
      # -------------------
      - name: Build React
        run: npm run build

      # -------------------
      # ✅ 5. Run tests
      # -------------------
      - name: Run React tests
        run: npm test
